@model IEnumerable<GiftShopV1.Models.FlowerModel>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.FlowerImage)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.FlowerName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.FlowerPrice)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.FlowerImage)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FlowerName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FlowerPrice)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.FlowerId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.FlowerId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.FlowerId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
