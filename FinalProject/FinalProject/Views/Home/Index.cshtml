@{
    ViewData["Title"] = "Home Page";
}

<div class="text-center">
    <h1 class="display-4">Welcome</h1>
    <button onclick="@("window.location.href='" + @Url.Action("", "Chat") + "'");" asp-controller="Chat" type="button" class="btn btn-dark">Start</button>

</div>
