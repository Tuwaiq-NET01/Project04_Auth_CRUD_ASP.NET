@{
    var Events = (List<EventsModel>)ViewData["Events"];
}



<h1 style="text-align: center; color:#807b74 ">Events</h1>


<div class="container">
    <div class="row">

        @foreach (var item in Events)
        {

            <div class="col-4" style="margin-top:10%; text-align:center">
                <div class="card mb-3">
                    <h3 class="card-header">@item.Name</h3>
                    <div class="card-body">
                        <img class="card-img-top" src="@item.Image" alt="Card image cap" height="200" width="80">
                    </div>

                    <div class="card-body">
                        <a href="/Events/Details/@item.Id" class="btn btn-dark">Read More</a>
                    </div>
                    
                </div>
            </div>


        }

    </div>
</div>
