@model IEnumerable<Book_Project.Models.PublisherModel>

<br />

<h1>Authors List</h1>

<br />

<a asp-action="Add" type="button" class="btn btn-primary">Add Publisher</a>

<!--Table-->
<table class="table table-hover table-fixed">

    <!--Table head-->
    <thead>
        <tr>
            <th>Id</th>
            <th>Name</th>
            <th>Books</th>
            <th>Options</th>
        </tr>
    </thead>
    <!--Table head-->
    <!--Table body-->
    <tbody>
        @{

            foreach (var BookPublisher in Model)
            {

                <tr>
                    <th scope="row">@BookPublisher.Id</th>

                    <td>@BookPublisher.Name</td>
                    <td>
                        @foreach (var Book in BookPublisher.Books)
                        {
                            <ul>
                                <li>@Book.Name</li>
                            </ul>

                        }
                    </td>

                    <td>
                        <div class="btn-toolbar" role="toolbar" aria-label="Toolbar with button groups">
                            <div class="btn-group mr-2" role="group" aria-label="First group">
                                <a asp-controller="Publishers" asp-action="Edit" asp-route-id="@BookPublisher.Id" type="button" class="waves-effect waves-light btn btn-warning" style="margin-right:5px"><i class="fas fa-edit" aria-hidden="true"></i></a>
                                <form method="post">
                                    <button asp-controller="Publishers" asp-action="Delete" asp-route-id="@BookPublisher.Id" type="submit" class="waves-effect waves-light btn btn-danger" style="margin-right:5px"><i class="fas fa-trash-alt" aria-hidden="true"></i></button>
                                </form>
                            </div>
                        </div>
                    </td>
                </tr>

            }

        }
    </tbody>
    <!--Table body-->

</table>
<!--Table-->